#include <stdio.h>

int is_letter(char c);

int main(){
    char c;
    
    scanf(" %c", &c);
    printf("> %d", is_letter(c));
}

int is_letter(char c){
    if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')) return 1;
    else return 0;
}
