#include <stdio.h>

char to_upper(char c);

int main(){
    char c;
    
    printf("Please enter a letter: ");
    scanf(" %c", &c);
    printf("%c", to_upper(c));
    
}

char to_upper(char c){
    if (c >= 'a' && c <= 'z'){
        return c - 32;
    }
    else {
        return 0;
    }
}
